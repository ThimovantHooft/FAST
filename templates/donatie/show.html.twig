{% extends 'base.html.twig' %}

{% block title %}Donatie{% endblock %}

{% block body %}
    <h1>Donatie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ donatie.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('donatie_index') }}">back to list</a>

    <a href="{{ path('donatie_edit', {'id': donatie.id}) }}">edit</a>

    {{ include('donatie/_delete_form.html.twig') }}
{% endblock %}
